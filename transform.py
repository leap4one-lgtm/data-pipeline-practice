def clean_numbers(numbers):
    """Remove duplicates and sort a list of numbers."""
    return sorted(set(numbers))

#d be tgv
def average(numbers):
    """Return the average of a list of numbers."""
    return sum(numbers) / len(numbers)
